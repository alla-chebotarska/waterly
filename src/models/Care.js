export default class Care {
    constructor(name, isActive, frequency, lastCare) {
        this.name = name;
        this.isActive = isActive;
        this.frequency = frequency;
        this.lastCare = lastCare;
    }
}